pipeline {
    agent any
    stages {
        stage('Clone Repo') {
            steps {
                sh 'git clone https://github.com/ramchamselenium/js-fs.git'
            }
        }
        stage("Build"){
            steps {
                dir("js-fs") {
                    sh "node access.js"
                }
            }
        }
    }
}