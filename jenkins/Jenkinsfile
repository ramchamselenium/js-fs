pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh 'node access.js'
            }
        }
        stage('Test') {
            steps {
                sh 'node accessSync.js'
            }
        }
    }
}